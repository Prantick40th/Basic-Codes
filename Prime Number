#include<stdio.h>
int main()
{
    int i , j , n ;
    printf("Enter a Number: ");
    scanf("%d",&n);

    for(i=2 ; i<n ; i++){
        j = n%i;
        if (j==0)
            break;
    }
    if(j==1)
        printf("Prime");
    else
        printf("Not Prime");

    return 0;
}
